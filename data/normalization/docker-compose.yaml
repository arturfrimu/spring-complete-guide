version: '3.8'

services:
  postgres:
    image: postgres:13
    container_name: normalization
    environment:
      POSTGRES_DB: normalization
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5411:5432"